技术路线方案

总体设计思路
本项目采用“数据驱动与大模型推理深度融合”的技术路线，构建以多模态视觉 - 语言大模型（VLM）为认知核心，以检索增强生成（RAG）与规则引擎为约束保障的智能解析系统。整体架构划分为数据预处理层、多模态解析引擎、逻辑校验层与业务应用层。系统摒弃传统 OCR 仅能识别文本的弱点，利用大模型的视觉理解能力，实现从“非结构化图纸像素”到“结构化工程数据”的一键转化。

核心技术流程（Pipeline）

系统运行流程遵循以下四个阶段：

第一阶段：图纸标准化预处理
针对来源多样的工程图纸（PDF、CAD 导出的图片、扫描件），系统首先进行版面分析与图像增强。对于大幅面图纸（A0/A1），采用自适应滑窗切片技术，在保持局部细节清晰度的同时，通过重叠切片避免图框连接处信息丢失。切片后的图像统一缩放至多模态模型的最佳输入分辨率。

第二阶段：多模态大模型并行解析
此阶段是系统的核心。预处理后的图像输入经 LoRA 微调的 Qwen2.5-VL-7B 模型。
1. 视觉特征提取：模型直接读取线条图、形位公差符号及表格结构，提取全局语义特征。
2. 信息生成：基于预设的结构化提示词（Prompt），模型并行提取关键信息，包括标题栏属性、BOM 物料清单、几何公差及技术要求文本。

第三阶段：知识检索与逻辑增强
为解决通用模型在特定工程领域知识匮乏的问题，系统引入检索增强技术（RAG）。当模型识别到特定国标代号（如 GB/T 1184）或特定牌号材料时，实时查询外挂的本地工程标准知识库，将检索到的正确释义注入模型上下文，确保术语解释与合规性审查的准确度。

第四阶段：规则引擎校正与输出
大模型的输出包含一定的不确定性（幻觉），系统引入规则引擎进行“兜底”校验：
1. 格式正则匹配：利用正则表达式强制约束数值格式（如 20±0.02）。
2. 逻辑一致性检查：校验 BOM 表数量累加是否与总数一致，单位是否统一。
3. 最终输出：经清洗的数据生成标准 JSON 格式，前端渲染为 Excel 表格或质检报告。

基座选型：选择 Qwen2.5-VL-7B，兼顾了强大的中文理解力与高分辨率视觉识别能力，且开源生态友好。
微调方法：采用 LoRA（低秩自适应）技术。冻结基座模型大部分参数，仅训练注入的低秩矩阵，显存占用降低 80% 以上。
数据构造：遵循“少而精”原则，构造约 500 条高质量指令微调数据。数据形式为 [图像，指令，思维链回复，最终答案]，重点强化材料识别、参数提取和合规性审查三类任务。
评估体系：建立自动化评测脚本，以提取准确率（Precision）和召回率（Recall）为指标，对比微调前后的 F1-Score，确保模型在实际部署中达到可用标准（目标指标 F1>0.9）。


